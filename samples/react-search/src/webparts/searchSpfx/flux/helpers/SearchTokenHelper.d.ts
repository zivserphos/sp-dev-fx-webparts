import { IWebPartContext } from '@microsoft/sp-webpart-base';
export default class SearchTokenHelper {
    private regexVal;
    constructor();
    replaceTokens(restUrl: string, context: IWebPartContext): string;
    private getDateValue;
    private getDaysVal;
    private getUserValue;
}
